# Puerto local (Render usa PORT automático)
PORT=3000

# Para producción: Render inyecta DATABASE_URL
# Para local, si quieres probar contra un Postgres local o remoto:
DATABASE_URL=postgres://usuario:password@localhost:5432/sigep_gc

# Orígenes permitidos para CORS (opcional, separa por coma)
CORS_ORIGIN=http://localhost:5500,http://localhost:3000
